import { baseTheme } from "@chakra-ui/react";

const theme = {
  ...baseTheme,
  styles: {
    global: {
      "html, body": {
        fontSize: "16px",
        background: "gray.50",
      },
    },
  },
};

export default theme;
